// Intentionally bad JS style: missing required semicolon to trigger linter semi rule
document.addEventListener('DOMContentLoaded', function() {
  const message = 'This script is deliberately written without a terminating semicolon' // <-- missing semicolon
  console.log(message)
})
